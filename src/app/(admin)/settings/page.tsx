import { UnderConstruction } from "@/components/admin/UnderConstruction";

export const metadata = {
  title: "Settings | Numi Villa",
};

export default function SettingsPage() {
  return <UnderConstruction />;
}
